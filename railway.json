{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'set -e; python3 scripts/prepare_voice.py; npm run render; test -s out/video.mp4; echo VIDEO_BOOT_RENDER_OK; uvicorn api_server:app --host 0.0.0.0 --port ${PORT}'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 600,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 2
  }
}
